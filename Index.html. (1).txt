<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>tactus - Scanner SMC & Sessions</title>
<style>
:root {
--bg-dark: #070712;
--card-bg: #111122;
--neon-purple: #9d4edd;
--neon-glow: rgba(157, 78, 221, 0.4);
--text-main: #ffffff;
--text-muted: #a0a0c0;
}

body {
background-color: var(--bg-dark);
color: var(--text-main);
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
margin: 0;
padding: 16px;
display: flex;
flex-direction: column;
align-items: center;
}

.container {
width: 100%;
max-width: 480px;
}

h1 {
font-size: 1.4rem;
text-align: center;
margin-bottom: 12px;
color: var(--neon-purple);
text-shadow: 0 0 10px var(--neon-glow);
}

.sessions-bar {
display: flex;
gap: 8px;
margin-bottom: 16px;
overflow-x: auto;
padding-bottom: 4px;
}

.session-btn {
background-color: var(--card-bg);
border: 1px solid var(--neon-purple);
color: var(--text-muted);
padding: 8px 12px;
border-radius: 8px;
font-size: 0.8rem;
white-space: nowrap;
cursor: pointer;
transition: all 0.2s;
}

.session-btn.active {
background-color: var(--neon-purple);
color: #fff;
box-shadow: 0 0 10px var(--neon-glow);
font-weight: bold;
}

.upload-card {
background-color: var(--card-bg);
border: 2px dashed var(--neon-purple);
border-radius: 16px;
padding: 20px;
text-align: center;
cursor: pointer;
box-shadow: 0 0 15px var(--neon-glow);
transition: all 0.3s ease;
}

input[type="file"] {
display: none;
}

.upload-text {
color: var(--text-muted);
font-size: 0.9rem;
margin-top: 8px;
}

#result-container {
display: none;
margin-top: 16px;
background-color: var(--card-bg);
border-radius: 16px;
padding: 16px;
box-shadow: 0 0 20px rgba(0,0,0,0.5);
}

.canvas-wrapper {
position: relative;
width: 100%;
border-radius: 8px;
overflow: hidden;
background: #000;
}

canvas {
width: 100%;
height: auto;
display: block;
}

.analysis-box {
margin-top: 16px;
background: #1a1a38;
padding: 12px;
border-radius: 8px;
border-left: 4px solid var(--neon-purple);
}

.verdict-header {
display: flex;
justify-content: space-between;
align-items: center;
font-weight: bold;
font-size: 1.05rem;
margin-bottom: 8px;
}

.badge-signal {
background-color: #2ec4b6;
color: #000;
padding: 4px 10px;
border-radius: 6px;
font-size: 0.85rem;
}

.stars {
color: #ffb703;
letter-spacing: 2px;
}

.details {
font-size: 0.82rem;
color: var(--text-muted);
line-height: 1.4;
}

.grid-prices {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 8px;
margin-top: 12px;
}

.price-card {
background: #111122;
padding: 8px;
border-radius: 6px;
border: 1px solid rgba(157, 78, 221, 0.2);
}

.price-label {
font-size: 0.7rem;
color: var(--text-muted);
}

.price-val {
font-size: 0.95rem;
font-weight: bold;
color: #fff;
}
</style>
</head>
<body>

<div class="container">
<h1>TACTUS // LA MAIN DE DIEU</h1>

<!-- Sélecteur de Sessions / Marchés -->
<div class="sessions-bar">
<button class="session-btn active" onclick="setSession('NY', this)">🇺🇸 New York (XAU/US30)</button>
<button class="session-btn" onclick="setSession('LDN', this)">🇬🇧 Londres (EUR/GBP)</button>
<button class="session-btn" onclick="setSession('ASIA', this)">🇯🇵 Asie (JPY/AUD)</button>
</div>

<!-- Zone d'importation -->
<label class="upload-card" id="drop-zone">
<svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#9d4edd" stroke